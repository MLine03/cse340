<h1><%= title %></h1>
<ul>
  <% inventory.forEach(vehicle => { %>
    <li>
      <a href="/inventory/<%= vehicle.inv_id %>">
        <%= vehicle.inv_make %> <%= vehicle.inv_model %>
      </a>
    </li>
  <% }) %>
</ul>