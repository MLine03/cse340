<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Inventory List</title>
</head>
<body>
  <h1>Vehicle Inventory</h1>
  <ul>
    <% vehicles.forEach(vehicle => { %>
      <li>
        <a href="/inventory/<%= vehicle.inv_id %>">
          <%= vehicle.make %> <%= vehicle.model %> (<%= vehicle.year %>)
        </a>
      </li>
    <% }) %>
  </ul>
</body>
</html>