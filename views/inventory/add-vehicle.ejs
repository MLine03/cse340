<!DOCTYPE html>
<html lang="en">
<head><title><%= title %></title></head>
<body>
  <h1>Add Vehicle</h1>
  <% if (message) { %><p style="color:red"><%= message %></p><% } %>
  <form action="/inv/add-vehicle" method="POST" novalidate>
    <label>Classification:</label>
    <%- classifications %>
    <label>Make:</label>
    <input type="text" name="inv_make" value="<%= sticky.inv_make || '' %>" required>
    <label>Model:</label>
    <input type="text" name="inv_model" value="<%= sticky.inv_model || '' %>" required>
    <label>Description:</label>
    <textarea name="inv_description" required><%= sticky.inv_description || '' %></textarea>
    <label>Price:</label>
    <input type="number" name="inv_price" value="<%= sticky.inv_price || '' %>" required>
    <label>Year:</label>
    <input type="number" name="inv_year" value="<%= sticky.inv_year || '' %>" required>
    <label>Miles:</label>
    <input type="number" name="inv_miles" value="<%= sticky.inv_miles || '' %>" required>
    <label>Color:</label>
    <input type="text" name="inv_color" value="<%= sticky.inv_color || '' %>" required>
    <button type="submit">Add Vehicle</button>
  </form>
</body>
</html>